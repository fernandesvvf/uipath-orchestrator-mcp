# deps
node_modules/

# logs
*.log
npm-debug.log*

# env / secrets (NUNCA versionar PAT)
.env
.env.*
!.env.example
*.local

# editor
.DS_Store
*.swp

# local machine MCP config (absolute paths / per-user)
.vscode/mcp.local.json

# build (se um dia houver)
dist/
build/
