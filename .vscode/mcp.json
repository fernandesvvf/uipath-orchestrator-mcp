{
  "//": "Open THIS folder (uipath-orchestrator-mcp) as the VS Code workspace so the relative paths resolve. Fill secrets in .env (gitignored). If you open a parent folder instead, copy this to .vscode/mcp.local.json and use absolute paths.",
  "servers": {
    "uipath-orchestrator-mcp": {
      "command": "node",
      "args": ["--env-file-if-exists=.env", "./src/index.ts"],
      "cwd": "${workspaceFolder}"
    }
  }
}
